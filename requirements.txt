# Core Django
Django==5.2.8
asgiref==3.11.0
sqlparse==0.5.3

# Environment & Configuration
python-dotenv==1.2.1
python-decouple==3.8

# Image Processing
Pillow==12.0.0

# Database Drivers (for production)
# PostgreSQL
# psycopg2-binary==2.9.10  # Uncomment for production with PostgreSQL

# MySQL/MariaDB (alternative)
# mysqlclient==2.2.6  # Uncomment for production with MySQL/MariaDB

# WSGI Server
gunicorn==23.0.0

# Static Files
whitenoise==6.11.0

# Security & CORS
django-cors-headers==4.9.0

# Monitoring (Optional - for production)
# sentry-sdk==2.20.0  # Uncomment if using Sentry

# Development Dependencies (install separately with pip install -r requirements-dev.txt)
# django-debug-toolbar==4.4.6
# coverage==7.6.10
